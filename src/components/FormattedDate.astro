---
interface Props {
  date: string | Date;
  className?: string;
}

const { date, className } = Astro.props;

import dayjs from "dayjs";
---

<time datetime={date.toString()} class={className}
  >{dayjs(date).format("D MMMM YYYY")}</time
>
